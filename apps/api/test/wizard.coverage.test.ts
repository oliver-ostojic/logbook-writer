import { describe, it } from 'vitest';

// Wizard routes have been retired; keep a placeholder test file so historical CI references
// don't go missing during the solver refactor branch, but don't execute any endpoint tests.
describe('wizard (retired)', () => {
  it('skipped', () => {
    // no-op
  });
});
